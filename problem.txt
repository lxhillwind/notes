- [windows] foobar2000 无法自动显示合适的专辑图片 (Front Cover / Disc Cover); 例如网易云音乐下载的部分文件为后者.

- [arch / iso] 安装脚本里未在最后设置 mirror, 有一定机率会被覆盖掉.

- [windows] 无需安装 7zip; 获取 checksum 的话, 可以用自带工具 (甚至 thinpc 里都有) certutil
(例如: `certutil -hashfile FILENAME MD5`; 注意 method 要大写!)

- [vim] windows vim 无法支持 `<range>Sh` (似乎是因为 term_start in_buf `<C-z>` 和 `<C-d>`).
    - eof_chars 搞定; 回显如何处理?

<!-- vim:ft=markdown -->
